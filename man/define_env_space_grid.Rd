% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/define_env_space_grid.R
\name{define_env_space_grid}
\alias{define_env_space_grid}
\title{Define factorial environmental space (grid of combinations)}
\usage{
define_env_space_grid(
  variables,
  n_levels = 2,
  context = c("field", "controlled"),
  default_within_sd = 0
)
}
\arguments{
\item{variables}{A named list, one element per environmental
variable, each containing at least \code{range = c(min, max)}.
Example:
\code{list(temperature = list(range = c(15, 30)), moisture    = list(range = c(20, 80)))}

Optionally, each variable list can also contain
\code{n_levels} (integer) and \code{within_group_sd} (numeric).}

\item{n_levels}{Either a single integer (same number of levels
for all variables) or a named integer vector with one value per
variable (names must match \code{variables}).
Ignored for variables that have their own \code{n_levels} entry.}

\item{context}{Character, \code{"field"} or \code{"controlled"}.}

\item{default_within_sd}{Numeric. Default within-group SD used
if neither the variable nor \code{within_group_sd} specifies a value.}
}
\value{
An \code{env_space} object.
}
\description{
This function creates an \code{env_space} object where each group
corresponds to a combination of levels across environmental
variables (e.g. low/high temperature × low/high moisture).
}
